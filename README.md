# jukctl

`jukctl` is a command line interface g-code sender for [JUK2](https://github.com/lysolaka/juk2). It takes an SVG file as an input and then uses the binary mode of the JUK2 interface to send motion commands.
